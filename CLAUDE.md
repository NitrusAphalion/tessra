# Agent notes

Generated by `tessra export` from shared memory. Do not edit; record with `tessra remember` and export again.

## This repository is under Tessra

Work through the `tessra` tools: `status` first, `context` for what you touch, `workspace --action adopt` to work in this checkout, then `snapshot`, `verify`, `promote`, and `remember` what you learn. `.git/` is the owner's bridge, not yours: do not `git add`, `commit`, `branch`, `checkout`, `stash`, `rebase`, or `push`. `promote` is the commit; the owner's `export` is the push, and the commit it writes is authored by you.

## Path: .github/workflows/release.yml

- **convention** (0.80): release.yml is generated by dist from dist-workspace.toml; change the toml and run dist generate, never edit the workflow by hand

## Path: BUGS.md

- **convention** (0.80): bugs are GitHub issues only, filed with gh issue create --body-file; BUGS.md is a frozen archive and nothing is appended to it; the fix workflow is in CONTRIBUTING.md

## Path: Cargo.toml

- **convention** (0.80): cut a release by bumping [workspace.package] version, committing, and pushing a vX.Y.Z tag; the Unreleased section of CHANGELOG.md becomes the release notes

## Path: crates/tessra-daemon/src/bridge.rs

- **decision** (0.80): git is optional at run time: only init's history import and the bridge verbs export --format git and import --branch run the git command

## Path: crates/tessra-daemon/src/lib.rs

- **convention** (0.80): every child process the daemon starts for itself goes through crate::quiet(Command::new(...)), which sets CREATE_NO_WINDOW on Windows so no console pops up; a new spawn site without it will open a window on every verify, export, or hook

## Path: crates/tessra-daemon/src/risk.rs

- **gotcha** (0.80): fixed in 36e2613: risk_sensitive accepts comma-separated globs from config --set, like state_paths; the earlier note that it could not be set from the CLI no longer applies

## Path: crates/tessra-daemon/src/verbs.rs

- **gotcha** (0.80): a landing refused for a weakened test charges the change's author 2 anomaly points on every frontier pass, and 6 points revoke the agent for good, so never retry a refused landing: get the approval attested first, then land once
- **gotcha** (0.80): fixed in 8fb2203: snapshot --then verify now runs the verifiers and attests, so the loop's single-call form works; binaries built before that commit only evaluate the standard
- **gotcha** (0.80): snapshot --then verify only reports the standard's status; run verify itself to run the verifiers and attest tests.pass
- **gotcha** (0.80): config --set stores text, bool, or int only, but risk_sensitive is read as an array, so it cannot be set from the CLI today

## Path: crates/tessra-oplog/src/standard.rs

- **gotcha** (0.80): test.weakened means any modified or deleted test, and a forbid with unless approved(human) opens no approval request; a human attests approval.human on the revision directly with tessra --as <human> attest --kind approval.human --subject <revision>
- **gotcha** (0.80): flags such as secrets are set on the snapshot revision only; the merged revision a landing creates carries none, so structural(flags.none) holds at landing even when the snapshot was flagged

## Repository

- **convention** (0.80): the workspace is rustfmt-formatted and the standard requires fmt.clean (cargo fmt --all --check) and lint.clean (clippy with -D warnings) besides tests.pass; run cargo fmt --all before snapshotting, and note that configuring any verifier turns off auto-detection so tests.pass must stay configured explicitly
- **gotcha** (0.80): revocation is permanent for an agent name; agent claude was auto-revoked on 2026-09-10 by repeated refused landings, so sessions in this repository act as claude-code
- **gotcha** (0.80): the daemon must be a released binary, never target/debug; test daemon changes with target/debug/tessra --no-daemon against a scratch repository
- **gotcha** (0.80): main has a GitHub ruleset requiring pull requests and signed commits; owner pushes bypass it with a warning, contributors cannot

