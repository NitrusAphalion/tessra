[package]
name = "tessra-daemon"
description = "Tessra daemon: repository, workspaces, sessions, verbs, git import"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
tessra-core = { path = "../tessra-core" }
tessra-oplog = { path = "../tessra-oplog" }
tessra-store = { path = "../tessra-store" }
tessra-semantic = { path = "../tessra-semantic" }
serde.workspace = true
serde_bytes.workspace = true
serde_json = "1"
ciborium.workspace = true
thiserror.workspace = true
hex.workspace = true
blake3.workspace = true

[dev-dependencies]
tempfile = "3"
